sudo: required
service:
  - docker
  

before_install:
  - docker build -t riteshbehl/docker-react -f DockerFile.Dev .
  
script:

  - docker run   riteshbehl/docker-react npm run test -- -- coverage
  
  